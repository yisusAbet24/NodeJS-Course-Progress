console.log(process.env.PORT ?? 3000);
const { SHELL } = process.env;
/* console.table({ SHELL });
 */
const person = ["jesus", "abet", "berenice"];
const [, , p3] = person;

/* const find = person.filter((name) => name === "jesus");
if (find.length == 0) console.log("Not find user");
console.log(find);
 */
